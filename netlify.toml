[build]
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

# SPA routing (wouter client-side routes)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
